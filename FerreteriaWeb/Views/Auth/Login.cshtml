@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model FerreteriaWeb.Models.LoginViewModel
@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <title>Iniciar Sesión</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css"/>
</head>
<body class="bg-light">
<div class="container mt-5" style="max-width: 400px;">
    <div class="card shadow border-0 rounded-4">
        <div class="card-body p-4">

            <h4 class="text-center fw-bold mb-1">🔧 Ferretería</h4>
            <p class="text-center text-muted small mb-4">Sistema de Inventario</p>

            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger">@ViewBag.Error</div>
            }

            <form method="post" asp-controller="Auth" asp-action="Login">
                <div class="mb-3">
                    <label class="form-label small text-muted">Correo</label>
                    <input asp-for="Email" class="form-control rounded-3" 
                           placeholder="correo@ejemplo.com" required/>
                </div>
                <div class="mb-4">
                    <label class="form-label small text-muted">Contraseña</label>
                    <input asp-for="Password" type="password" class="form-control rounded-3" 
                           placeholder="••••••" required/>
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn btn-dark rounded-3">Ingresar</button>
                </div>
            </form>

        </div>
    </div>
</div>
</body>
</html>